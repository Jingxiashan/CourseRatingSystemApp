<?xml version="1.0" encoding="utf-8"?>
<shape
    xmlns:android="http://schemas.android.com/apk/res/android"

    android:shape="rectangle">
    <!-- 实心 -->
    <solid android:color="@color/pureWhite"/>
    <!-- 边框 -->
    <stroke
        android:width="1dp"
        android:color="@color/lightGreyAlpha"/>
    <!-- 圆角 -->
    <corners android:radius="5dp"/>
    <!-- 边距 -->
    <padding
        android:bottom="2dp"
        android:left="4dp"
        android:right="4dp"
        android:top="2dp"/>
    <!-- 渐变 -->
    <!--<gradient-->
        <!--android:angle="270"-->
        <!--android:endColor="@color/pureBlack"-->
        <!--android:startColor="@color/pureWhite"/>-->
</shape>